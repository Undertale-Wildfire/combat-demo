if (!is_undefined(below_surface)) {
	surface_free(below_surface);
}

if (!is_undefined(above_surface)) {
	surface_free(above_surface);
}

if (!is_undefined(outline_surface)) {
	surface_free(outline_surface);
}